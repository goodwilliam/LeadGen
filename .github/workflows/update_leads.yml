name: Update Leads

on:
  schedule:
    - cron: "0 9 * * *"   # 9 AM UTC daily
  workflow_dispatch:        # allow manual trigger from Actions tab

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Fetch YC companies
        run: python fetch_yc.py

      - name: Fetch crypto signals
        run: python fetch_crypto.py

      - name: Fetch top 1000 MCap crypto
        run: python fetch_crypto_top.py

      - name: Fetch seed funding news
        run: python fetch_seed.py

      - name: Commit and push if data changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/yc.json data/crypto.json data/crypto_enrichment_cache.json \
                  data/crypto_top.json data/crypto_top_enrichment_cache.json \
                  data/seed.json data/seed_enrichment_cache.json
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "chore: update leads $(date -u +%Y-%m-%d)"
            git pull --rebase origin main
            git push
          fi
